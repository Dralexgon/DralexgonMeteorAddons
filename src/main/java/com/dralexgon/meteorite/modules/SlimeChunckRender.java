package com.dralexgon.meteorite.modules;

public class SlimeChunckRender {

    //ChunkRandom.getSlimeRandom(chunkPos.x, chunkPos.z, ((StructureWorldAccess)world).getSeed(), 987234911L).nextInt(10) == 0;
    //private final Setting<SettingColor> lC2 = sgColor.add(new ColorSetting.Builder()
    //        .name("hologram-line-color")
    //        .description("The color of the lines of the blocks being rendered.")
    //        .defaultValue(new SettingColor(255, 255, 0, 100))
    //        .build()
    //    );
}
